export const TERMS_DATA = Array.from({ length: 57 }, (_, i) => ({
    id: i + 1,
    t: `بند قانوني رقم ${i + 1}`,
    c: "هذا النص يمثل أحد الشروط والأحكام المعتمدة في منصة في خدمتك لضمان حقوق الطرفين."
})).map((item, index) => {
    // أمثلة لبعض البنود الفعلية
    if(index === 0) item.c = "العرض صالح لمدة 3 أيام من تاريخ الإصدار.";
    if(index === 9) item.c = "تخضع الأسعار لضريبة القيمة المضافة 15%.";
    if(index === 36) item.c = "تختص محاكم مدينة حائل بالنظر في أي نزاع.";
    return item;
});
